
resources:
  - name: incoming_webhook_rb_res
    type: IncomingWebhook
    configuration:
      webhookName: incoming_webhook_RB_signed
 
pipelines:
  - name: incoming_webhook_RBS
    steps:
      - name: start_by_hook
        type: bash
        configuration:
          inputResources:
            - name: incoming_webhook_rb_res
        execution:
        onStart:
          - echo "start runing RB signed demo"

          onExecute:
            - echo "$res_incoming_webhook_RBS_payload" | jq '.' > payload.json
            - read_json payload.json "Hello"
